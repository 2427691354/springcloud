server:
  port: 8400
zuul:
  routes:
    test: /myTest/**
management:
  endpoints:
    web:
      exposure:
        include: ["routes"]

feign:
  hystrix:
    enabled: true # 开启Feign熔断
  client:
    config:
      default:
        connectTimeout: 3000
        readTimeout: 3000
